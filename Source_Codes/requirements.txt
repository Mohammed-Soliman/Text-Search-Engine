flask==3.1.3
gunicorn==23.0.0
nltk==3.9.1
numpy==2.4.4
pandas==3.0.2
tqdm==4.67.1

# SBERT re-ranking is optional. It pulls in torch, which is large (100s of
# MB) and will make builds slow and hosting memory-hungry. Comment the next
# two lines out if you don't need the SBERT method/expansion - the rest of
# the app (BM25 / TF-IDF / VSM / Boolean, spelling correction, relevance
# feedback) all work fine without it.
sentence-transformers==3.3.1
torch==2.5.1
